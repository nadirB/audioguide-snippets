'use babel';

import AudioguideSnippetsView from '../lib/audioguide-snippets-view';

describe('AudioguideSnippetsView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
